// import { Olympiad } from './types';

// Функція, що перевіряє, чи активна олімпіада (за поточним часом)
// export const isOlympiadActive = (olympiad: Olympiad): boolean => {
//   const now = new Date();
//   const start = new Date(olympiad.startDate);
//   const end = new Date(olympiad.endDate);
//   return now >= start && now <= end;
// };

// Функція для форматування дат олімпіади для відображення
// export const formatOlympiadDates = (olympiad: Olympiad): string => {
//   const start = new Date(olympiad.startDate);
//   const end = new Date(olympiad.endDate);
//   return `${start.toLocaleDateString()} - ${end.toLocaleDateString()}`;
// };
